$schema: "../../schemas/binding.schema.json"
id: BIND-AGT-003-AGENT
control_id: AGT-003
technology_context: agent
introduced_date: "2026-07-10"
introduced_by: platform-team
status: active
specification: >
  .vscode/mcp.json (when present) must be valid JSON and contain no hardcoded credentials;
  secrets must use ${input:…} / ${env:…} references.
observable_artifact: "collect-agent-info.py output fields: mcp.config_valid, mcp.hardcoded_secret_suspected"
policy_check_ids:
  - POL-AGT-003-AGENT-001
